logging system :
 const WrappedUser = db.wrapModel(User, {
  logs: {
    name: "user_history_logs"
  }
});



------------------------------------------------------------------------------------------------------------


good but 2-3 problems one is better use already made module for getting page dont rewrite the full logic just moduole can be modified to also retrun just the no of document if its correctly made and secondly instead of importing mongoDB client for small things or making a whole model for small things cant ge make a generalised model or zustand like store like 

model will be : { _id ofc mongoose will give that then , name:'for eg here it can be admin-credentials' , data:{whatever data we wanna store here , } ,  } and make a global store funtion like src/store/index.ts , internal.ts , schemas/adminCredentials.ts for eg internal will be all logic of that model import and etc and in any of schema we can make like import dbStore from src/internal.ts  then export const adminCredentials = dbStore.univarsalCreate({db:'admin' , name:'admin-credentials' , schema:a zod schema here for eg : {z.string for username , password }}) then later imported from store/index.ts and re exported for better codebase, then any where we can do like  await adminCredentials.set({username:"123" , password:"1234"}) or adminCredentials.get() or adminCredentials.del()

this is ok for univarsal but if we need some dynamic store than we need to do something like petrolPrice = dbStore.create({name:'petrol-price' , schema:zod same  }) then later petrolPrice.db('test-1-db-name').set() .get etc 

dont code just first clear all this with me

Show less
18:38

Clarified generic MongoDB store architecture with universal and dynamic database patterns
Clarified generic MongoDB store architecture with universal and dynamic database patterns
Good thinking. Let me clarify before coding. Here's my understanding, tell me what's wrong:

The universal MongoDB model shape:

collection: kv_store  (one collection for everything)
document:   { _id, name: 'admin-credentials', data: { username, password } }
universalCreate — db fixed + name fixed → true singleton:

ts
adminCredentials.get()         // always admin db, admin-credentials doc
adminCredentials.set({...})    // validates against zod schema, upserts
adminCredentials.del()
create — name fixed, db dynamic → per-firm data:

ts
petrolPrice.db('firm-123').get()
petrolPrice.db('firm-123').set({price: 100})
Questions before coding:

All these live in one collection (kv_store) differentiated by name field, or does each name get its own collection? Own collection is cleaner for indexing but one collection is simpler.
For universalCreate the Zod schema validates data shape — so adminCredentials.set({username: 'x', password: 'y'}) writes { name: 'admin-credentials', data: { username: 'x', password: 'y' } }? Meaning Zod validates the contents of data, not the whole document?
Does get() return the raw data object or the full document including _id and name?
For create (dynamic db) — is name still a fixed string like 'petrol-price', just the db changes per call?